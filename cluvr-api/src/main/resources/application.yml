spring:
  redis:
    host: localhost # 또는 host.docker.internal
    port: 6379
  profiles:
    active: local
  datasource:
    url: jdbc:mysql://localhost:3306/test
    username: root
    password: ghks9430
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: update # 또는 create, create-drop, validate 중 선택
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQL8Dialect
        generate_statistics: true

jwt:
  secret:
    key: BvdCDROpy/QRNPizw10GXalXzl41f/YOK2SfJNK2s+w=
management:
  endpoint:
    health:
      show-details: always
  endpoints:
    web:
      exposure:
        include: "*"
      base-path: /my-monitor
  server:
    port: 9292
  prometheus:
    metrics:
      export:
        enabled: true

